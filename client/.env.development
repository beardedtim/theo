# Local `pnpm dev` default -- matches Vite's default dev server port.
# Overridden in the prod Docker build (see Dockerfile) to the real domain,
# since og:url/og:image must be absolute URLs for link unfurling to work.
VITE_SITE_URL=http://localhost:5173
